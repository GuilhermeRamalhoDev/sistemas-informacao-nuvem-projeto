# Copia para terraform.tfvars e preenche. NÃO commitar o terraform.tfvars real.
# A password da BD deve vir de um GitHub Secret (TF_VAR_db_password), não daqui.

key_name         = "o-nome-do-teu-key-pair"
ssh_ingress_cidr = "1.2.3.4/32"   # o teu IP público, em vez de 0.0.0.0/0
# db_password    = "definido via TF_VAR_db_password no CI"
